#include <stdio.h>

/**
 * main - this is the main function for the printf function
 * Return: always zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
